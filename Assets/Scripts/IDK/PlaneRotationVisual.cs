using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlaneRotationVisual : MonoBehaviour
{
    //[SerializeField] Rigidbody ReferenceRigidbody; 
    //[SerializeField] AccelerometerTest accelerometerScript;

    //[SerializeField] float PitchMaxAngle = 50;
    //[SerializeField] float RollMaxAngle = 160;
    
    //void Update()
    //{
    //    Vector3 accel = accelerometerScript.accel;
    //    Quaternion targetRotation = Quaternion.Euler(new Vector3(-accel.z * PitchMaxAngle,0,-accel.x * RollMaxAngle));
    //    transform.localRotation = Quaternion.Lerp(transform.localRotation, targetRotation, 5 * Time.deltaTime);
    //}
}
